'use strict';

describe('Directive: quickField', function () {

  // load the directive's module
  beforeEach(module('quickFormApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

//  it('should make hidden element visible', inject(function ($compile) {
//    element = angular.element('<quick-field></quick-field>');
//    element = $compile(element)(scope);
//    expect(element.text()).toBe('this is the quickField directive');
//  }));


  expect(true);


});
